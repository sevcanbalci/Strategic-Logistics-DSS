<!DOCTYPE html>
<html lang="tr">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Yönetici Girişi</title>
  <link rel="stylesheet" href="/css/style.css" />
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
  <style>
    .auth-wrapper{min-height:100vh;display:flex;align-items:center;justify-content:center;padding:24px}
    .auth-card{width:100%;max-width:420px;background:linear-gradient(180deg,#102740,var(--card));border:1px solid var(--border);border-radius:18px;box-shadow:0 20px 42px var(--shadow);padding:26px}
    .auth-brand{font-weight:800;font-size:22px;margin-bottom:6px;letter-spacing:.6px;background:linear-gradient(90deg,var(--accent),var(--accent-2));-webkit-background-clip:text;background-clip:text;-webkit-text-fill-color:transparent;text-align:center}
    .auth-title{color:#f0f7ff;text-align:center;margin:0 0 18px 0;font-size:20px}
    .form-group{margin-bottom:14px}
    .form-label{display:block;color:var(--muted);font-size:13px;margin-bottom:8px}
    .form-control{width:100%;padding:12px 14px;border-radius:10px;border:1px solid var(--border);background:#0e2338;color:var(--text);outline:none;transition:border-color .18s ease, box-shadow .18s ease}
    .form-control:focus{border-color:#1a79da;box-shadow:0 0 0 3px rgba(74,160,255,0.15)}
    .btn{width:100%;padding:12px 14px;border:none;border-radius:10px;background:linear-gradient(90deg,#1f7adb,#4aa0ff);color:#fff;font-weight:700;cursor:pointer;transition:transform .1s ease, box-shadow .2s ease}
    .btn:hover{box-shadow:0 8px 22px var(--shadow)}
    .btn:active{transform:scale(.99)}
    .auth-hint{margin-top:14px;color:#a6b5c8;font-size:12px;text-align:center}
    .error{margin-top:10px;color:#ff9da7;background:#2a0f14;border:1px solid #5a1f26;padding:10px;border-radius:10px;font-size:13px;text-align:center}
  </style>
</head>
<body>
  <div class="auth-wrapper">
    <div class="auth-card">
      <div class="auth-brand">Aktif Dekor Organizasyon</div>
      <h2 class="auth-title">Yönetici Girişi</h2>
      <form method="post" action="/login" novalidate>
        <div class="form-group">
          <label class="form-label" for="email">E-posta</label>
          <input class="form-control" id="email" name="email" type="email" placeholder="e-mail" autocomplete="username" required />
        </div>
        <div class="form-group">
          <label class="form-label" for="password">Şifre</label>
          <input class="form-control" id="password" name="password" type="password" placeholder="••••••••" autocomplete="current-password" required />
        </div>
        <button class="btn" type="submit">Giriş Yap</button>
        <% if (typeof error !== 'undefined' && error) { %>
          <div class="error"><%= error %></div>
        <% } %>
      </form>
      <div class="auth-hint">Yalnızca yetkili kullanıcılar giriş yapabilir</div>
    </div>
  </div>
</body>
</html>

